# Historical technical reviews

This directory preserves dated reviews and the response history that motivated
major revisions to the models and papers. Paths and filenames quoted inside the
reviews refer to the repository layout that existed when each review was
written; they are historical records and are not silently rewritten to match
the current organization.

Current code, papers, inputs, outputs, and validation materials are identified
from the top-level `README.md`.